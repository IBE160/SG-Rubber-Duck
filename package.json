{
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "start:backend": "python -m uvicorn backend.main:app --reload --port 8002",
    "start:all": "start start_dev.bat"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "react-bits": "^1.0.5"
  },
  "devDependencies": {
    "shadcn": "^3.5.0"
  }
}
