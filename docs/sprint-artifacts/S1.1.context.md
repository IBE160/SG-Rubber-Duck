# Story S1.1 Context: Initialize FastAPI app and basic configuration

## Story Overview
**Story ID:** S1.1
**Title:** Initialize FastAPI app and basic configuration
**Epic:** E1: Backend & Data Foundations

## Technical Context

### 1. Application Structure
The FastAPI application will reside within the `backend/` directory.
Initial folder structure:
```
backend/
├── main.py             # FastAPI application entry point
├── config.py           # Application settings/configuration
├── requirements.txt    # Python dependencies
├── .env.example        # Example environment variables
└── tests/              # Placeholder for tests
```

### 2. `main.py` - FastAPI App Entry Point

```python
# backend/main.py
from fastapi import FastAPI
from .config import settings

app = FastAPI(
    title=settings.app_name,
    version=settings.app_version,
    description=settings.app_description,
    debug=settings.debug_mode
)

@app.get("/", tags=["Health"])
async def root():
    return {"message": "FastAPI is running!"}

@app.get("/health", tags=["Health"])
async def health_check():
    return {"status": "ok", "version": settings.app_version}
```

### 3. `config.py` - Application Settings

Using Pydantic's `BaseSettings` for robust configuration management. This allows loading settings from environment variables with sensible defaults.

```python
# backend/config.py
from pydantic_settings import BaseSettings, SettingsConfigDict

class Settings(BaseSettings):
    app_name: str = "IBE160 Simulator Backend"
    app_version: str = "0.1.0"
    app_description: str = "Backend for IBE160 Project Simulator"
    debug_mode: bool = False

    # Database settings (placeholders for E1, will be detailed in future stories)
    database_url: str = "sqlite:///./test.db" # Default for local dev/testing
    
    model_config = SettingsConfigDict(env_file=".env")

settings = Settings()
```
*Note: `pydantic-settings` needs to be installed (`pip install pydantic-settings`).*

### 4. `requirements.txt`

```
# backend/requirements.txt
fastapi
uvicorn[standard]
pydantic>=2.0.0
pydantic-settings
```

### 5. `.env.example`

```
# .env.example
APP_NAME="IBE160 Simulator Backend"
APP_VERSION="0.1.0"
APP_DESCRIPTION="Backend for IBE160 Project Simulator"
DEBUG_MODE=True

# DATABASE_URL="postgresql://user:password@host:port/dbname"
```

## Setup & Running Instructions

1.  **Navigate to backend directory:** `cd backend`
2.  **Create virtual environment (if not already):** `python -m venv venv`
3.  **Activate virtual environment:**
    *   Linux/macOS: `source venv/bin/activate`
    *   Windows: `.\venv\Scripts\activate`
4.  **Install dependencies:** `pip install -r requirements.txt`
5.  **Create `.env` file:** Copy `.env.example` to `.env` and adjust settings as needed.
6.  **Run FastAPI application:** `uvicorn main:app --reload`
    *   The `--reload` flag enables auto-reloading on code changes during development.
7.  **Access:** Open your browser or use a tool like Postman/curl to access `http://127.0.0.1:8000/` or `http://127.0.0.1:8000/health`.

## Dependencies

*   Python 3.8+
*   `fastapi`
*   `uvicorn`
*   `pydantic`
*   `pydantic-settings`

## Considerations
*   For now, the `database_url` in `config.py` defaults to a SQLite in-memory database. This will be updated in subsequent stories when PostgreSQL integration is fully implemented.
*   The `main.py` imports `config`, establishing a clear separation of concerns.
*   Basic `tags` are used for API documentation organization.
